import React, {Component} from 'react';
import {Button, Input} from "antd";

class Home extends Component {
    render() {
        return (
            <div>
                <Button type="primary">asdsad</Button>
                <Input size="large" placeholder="large size" />
            </div>
        );
    }
}

export default Home;